services:
  sudoku-web:
    build: .
    image: sudokusolver-sudoku-web
    container_name: sudokusolver
    restart: unless-stopped
    ports:
      - 8011:8080
    command: uvicorn web.main:app --host 0.0.0.0 --port 8080
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
